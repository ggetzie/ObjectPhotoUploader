<Page
    x:Class="ObjectPhotoUploader.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ObjectPhotoUploader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <RelativePanel RequestedTheme="Default">
            <StackPanel x:Name="LoginPanel" Margin="20" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True">
                <TextBlock Text="Please log in" FontSize="36"/>
                <TextBox Name="Username" Header="Username" FontSize="18"/>
                <PasswordBox Name="Password" Header="Password" FontSize="18"/>
                <Button Content="Log In" HorizontalAlignment="Right" Margin="0,20,0,0" />
            </StackPanel>
        </RelativePanel>

    </Grid>
</Page>
